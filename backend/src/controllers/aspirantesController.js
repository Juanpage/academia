/**
 * ARCHIVO ELIMINADO — CÓDIGO MUERTO
 *
 * Este controller fue reemplazado completamente por:
 *   - GET  /api/aspirantes           → src/routes/aspirantes.routes.js
 *   - GET  /api/aspirantes/:id       → src/routes/aspirantes.routes.js
 *   - GET  /api/aspirantes/:id/dashboard → src/routes/aspirantes.routes.js
 *   - PUT  /api/aspirantes/:id       → src/routes/aspirantes.routes.js
 *
 * Diferencias detectadas con el esquema actual de BD:
 *   - Usaba tabla "aspirantes"      → BD real usa "aspirants"
 *   - Usaba columna "nombre/apellido" → BD real usa "first_name/last_name"
 *   - Usaba tabla "scores_globales" → BD real usa "global_scores"
 *   - Usaba tabla "pagos"           → BD real usa "payments"
 *   - Usaba tabla "planes_pago"     → BD real usa "payment_concepts"
 *   - Usaba tabla "pruebas_fisicas" → BD real usa "physical_evaluations"
 *   - Usaba tabla "academic_scores" → BD real usa "academic_evaluations"
 *
 * NO importar este archivo. Puede eliminarse del repositorio.
 */
throw new Error('aspirantesController está obsoleto. Ver src/routes/aspirantes.routes.js');
