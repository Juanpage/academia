/**
 * ARCHIVO ELIMINADO — CÓDIGO MUERTO
 *
 * Este controller fue reemplazado completamente por las rutas activas:
 *   - POST /api/auth/login         → src/routes/auth.js
 *   - POST /api/auth/logout        → src/routes/auth.js
 *   - POST /api/auth/register      → src/routes/auth.routes.js
 *   - GET  /api/auth/me            → src/routes/auth.js
 *
 * Diferencias detectadas con el esquema actual de BD:
 *   - Usaba tabla "usuarios"  → BD real usa "users"
 *   - Usaba tabla "aspirantes" → BD real usa "aspirants"
 *   - Usaba columna "rol"     → BD real usa "role"
 *   - Usaba columna "activo"  → BD real usa "is_active"
 *
 * NO importar este archivo. Puede eliminarse del repositorio.
 */
throw new Error('authController está obsoleto. Ver src/routes/auth.js y src/routes/auth.routes.js');
